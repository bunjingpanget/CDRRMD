export type AlertItem = {
  id: number;
  title: string;
  body: string;
  category: string;
  severity: string;
  created_at: string;
};

export type AnnouncementItem = {
  id: number;
  title: string;
  body: string;
  created_at: string;
};
